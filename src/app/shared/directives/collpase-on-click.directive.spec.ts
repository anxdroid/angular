import { CollpaseOnClickDirective } from './collpase-on-click.directive';

describe('CollpaseOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CollpaseOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
